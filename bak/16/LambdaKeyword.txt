Лямбда по сути своей, это просто указатель на функцию.

Создаем функцию с лямбдой
X = lambda do |a|

end

Лямбды нужны, потому что иногда переменным очень удобно присваивать указатели на какие либо функции, потому что если у нас есть массив мы можем туда поместить множество указателей на функцию а затем вызывать.
Вызывается функция следующим образом x.call a